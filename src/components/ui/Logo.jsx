import KeralaPetsLogo from './KeralaPetsLogo';

export default function Logo({ size = 48, showText = true, layout = 'horizontal' }) {
  return <KeralaPetsLogo size={size} showText={showText} layout={layout} />;
}

